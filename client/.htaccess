<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Serve index.html for all requests except actual files/folders
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^.*$ /index.html [L]
</IfModule>
